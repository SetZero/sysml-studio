using Avalonia.Controls;

namespace SysmlStudio.App.Views;

/// <summary>The export dialog: format, file, and how much of the model goes into it.</summary>
public sealed partial class ExportDialogView : UserControl
{
    public ExportDialogView() => InitializeComponent();
}
